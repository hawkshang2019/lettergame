# Netlify配置文件

[build]
  command = "npm run build"
  publish = "dist"

# SPA重定向规则 - 支持React Router
[[redirects]]
  from = "/admin"
  to = "/index.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
